# Excel Workflow Platform 路线图

## 目标架构

桌面 EXE 继续采用 pywebview + FastAPI + SQLite + openpyxl + 本地线程池。Excel 计算通过可插拔适配器处理，桌面 Windows 优先使用 Microsoft Excel COM，用户未安装 Excel 时尝试 LibreOffice，两个引擎都不可用时保留公式并返回明确状态。

## 实施顺序

1. Excel/LibreOffice 自动重算（已落地）
2. 公式结果预览（已落地）
3. 公式依赖分析（已落地）
4. 数据错误报告（已落地）
5. 批量 ZIP 下载（已落地）
6. 模板版本管理（已落地）
7. 模式 B ReactFlow 流程设计器和 DAG 执行器（最小闭环已落地）

## 阶段验收

### 阶段 1：自动重算（已落地）

- 生成任务保存公式文件
- Windows 检测 Excel COM 并执行重算
- 无 Excel 时检测 LibreOffice 并执行重算
- 返回 `excel`、`libreoffice` 或 `formula_only` 计算引擎状态
- 计算失败写入任务日志，保留可打开的公式文件

### 阶段 2：公式结果预览（已落地）

- 展示公式单元格、公式文本和计算值
- 支持多 Sheet
- 支持结果错误标记

### 阶段 3：公式依赖分析（已落地）

- 解析公式引用的单元格、列和 Sheet
- 生成公式列到输入字段的依赖关系
- 映射页面提示依赖字段

### 阶段 4：数据错误报告（已落地）

- 报告必填、类型、日期、数值、枚举和重复值错误
- 支持下载错误报告 Excel

### 阶段 5：批量 ZIP 下载（已落地）

- 批量任务完成后打包输出文件
- 支持单个失败文件继续保留失败日志

### 阶段 6：模板版本管理（已落地）

- 模板版本号和父模板关联
- 工作流固定绑定模板版本
- 新模板版本影响检查

### 阶段 7：模式 B（最小闭环已落地）

- ReactFlow 节点编排
- DAG 拓扑校验、循环检测和执行
- 数据源、字段映射、公式计算、条件判断、写入模板、输出文件节点
- 公式节点支持字段、数字、字符串和四则运算
- 节点配置支持表单编辑，数据源节点支持实际数据源选择和字段映射自动提示
- 稳定性修复覆盖 EXE 静态资源加载、数据源一致性、字段引用校验、DAG 输出连通性、CSV 数值转换和 ReactFlow 节点删除
- 桌面渲染器支持 WebView2 优先、MSHTML 回退
