# 无公式通报模板映射与计算实施清单

## 1. 样例与契约

- [ ] 将无公式样例登记为模板版本测试资产。
- [ ] 扩展模板解析结果，区分模板保留值列、可写列、指标列和合计列。
- [ ] 保存通报区域、明细字段、日期候选、组织候选和产品候选。
- [x] 将 `BA`（下沉区县名称）设为组织字段高优先级候选。
- [x] 将 `AZ`（下沉规则）识别为配套条件字段，并展示“发展人”等规则值。
- [ ] 保留 `H`、`AG` 作为可选组织字段候选并展示匹配差异。

## 2. 映射模型

- [x] 新增通报列映射模型和持久化接口。
- [ ] 新增指标规则模型，支持聚合、过滤、日期范围、比率、排名和合计。
- [ ] 新增组织维度绑定和别名映射模型。
- [x] 扩展 MappingSnapshot 保存列映射、指标规则、维度绑定和公式计划。

## 3. 计算引擎

- [x] 实现日期、数字和文本规范化。
- [x] 实现组织维度解析和多值匹配。
- [x] 实现 `sum`、`count`、`count_distinct`、`avg` 和 `max`。
- [x] 实现比率、排名和合计行；序时完成率规则保留为下一步派生指标扩展。
- [x] 实现产品条件与日/月周期过滤。
- [x] 输出每个通报单元格的匹配行数、来源规则和计算追踪。

## 4. Excel 公式处理

- [x] 设计统一指标 AST 和依赖拓扑排序。
- [x] 实现 AST 到值计算计划的编译器。
- [x] 实现 AST 到 `SUMIFS`、`COUNTIFS`、比率和排名公式的发射器。
- [ ] 使用动态明细范围或 Excel Table，避免固定行数。
- [ ] 增加公式白名单、引用校验和外部引用阻断。
- [ ] 保存公式预期值和待 Excel 重算状态。

## 5. 前端映射中心

- [x] 按模板 Sheet 和通报列展示映射清单。
- [ ] 增加直接字段、指标、公式、常量和模板保留值配置。
- [x] 增加组织字段候选、别名和匹配预览。
- [x] 增加指标规则编辑器和公式依赖详情。
- [x] 增加值计算、公式输出两种执行模式选择。
- [x] 增加按单元格查看计算追踪和预期值。

## 6. 生成与校验

- [x] 生成前校验字段签名、组织覆盖、产品覆盖、日期范围和规则版本。
- [ ] 阻止未确认列映射、缺失来源字段、循环依赖和公式覆盖风险。
- [x] 将值计算或公式输出模式写入任务审计。
- [ ] 生成后校验 Sheet、样式、合并区域、数值类型和公式引用。

## 7. 测试

- [ ] 增加无公式样例解析测试。
- [ ] 增加组织字段候选和别名匹配测试。
- [ ] 增加指标聚合、日期边界、产品过滤和排名测试。
- [x] 增加值计算与公式计划一致性测试。
- [ ] 增加缺失映射、零分母、空组织和数据类型错误测试。
- [ ] 运行 `PYTHONPATH=backend python3 -m pytest backend/tests -q`。
- [ ] 运行 `npm --prefix /workspace/frontend run build`。
- [ ] 运行 `git diff --check`。
