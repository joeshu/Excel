# 模板与基础数据映射中心实施计划

## 1. 梳理现有映射边界

- [x] 盘点模板解析、基础字段解析、模式 A 映射、模式 B DAG 和生成器的现有输入输出。
- [x] 明确现有 `node_json`、模板字段配置和任务配置中可复用的映射数据。
- [x] 列出旧配置到统一 `MappingRule` 的兼容转换方式。
- [x] 确认模板 Sheet 的表头行、数据起始行、公式列和必需列判定规则。

## 2. 建立统一映射模型

- [x] 新增模板输出列 schema，覆盖 Sheet、列地址、表头、类型和公式。
- [x] 新增基础数据字段 schema，复用字段类型和字段签名。
- [x] 建立统一映射规则的兼容服务表示，覆盖直接字段和公式规则。
- [x] 新增持久化 `MappingSnapshot` 模型，保存数据源字段签名、规则版本、依赖顺序和校验结果。
- [x] 增加旧工作流配置到统一映射视图的兼容读取测试。

## 3. 实现映射分析与预检服务

- [x] 实现模板输出列标准化接口。
- [x] 实现基础字段标准化接口。
- [x] 实现字段名称规范化和类型兼容评分。
- [x] 实现自动推荐候选字段、推荐原因和待确认状态。
- [x] 实现公式 AST 解析、字段依赖提取、循环依赖检测和稳定拓扑排序。
- [x] 实现目标列重复、缺失字段、类型冲突和模板公式覆盖校验。
- [x] 实现映射预检聚合结果，区分错误、警告和通过。

## 4. 接入模式 A 和模式 B

- [x] 为模式 A 增加统一映射规则适配器。
- [x] 将模式 A 的直接字段映射、模板公式和工作流公式转换为统一规则。
- [x] 将模式 B DAG 的字段映射、公式节点、条件节点和输出节点转换为统一只读视图。
- [x] 确保两种模式生成相同字段命名、依赖字段和目标列结构的快照。
- [x] 保留现有执行器作为实际计算入口，统一映射视图用于配置、预检和审计。

## 5. 实现映射中心 API

- [x] 增加临时规则预览和校验接口。
- [x] 增加模板映射结构查询接口。
- [x] 增加基础数据字段查询接口。
- [x] 增加独立映射规则保存、查询和版本接口。
- [x] 增加映射快照创建和查询接口。
- [x] 增加现有模式 A/模式 B 工作流的统一映射视图接口。
- [x] 将任务提交改为接收已校验的映射快照引用。

## 6. 实现前端映射中心

- [ ] 增加以模板 Sheet 和输出列为主线的映射页面。
- [ ] 增加基础字段搜索、类型、示例值和质量状态展示。
- [ ] 增加直接字段、公式、常量、条件和空值策略编辑器。
- [ ] 增加公式依赖图、依赖字段详情和错误定位。
- [ ] 增加精确匹配、候选匹配、待确认、警告和错误状态。
- [ ] 增加映射完成度、预检摘要、保存版本和返回定位入口。
- [ ] 适配窄屏表格横向滚动和映射详情保留状态。

## 7. 改造生成和结果追溯

- [x] 生成向导增加映射确认步骤，并在预检失败时阻止提交。
- [x] 任务开始时保存不可变映射快照。
- [x] 在“工作流配置”Sheet 中写入逐列来源、公式、依赖、转换和校验结果。
- [x] 在结果预览中增加按模板输出列查看来源的入口。
- [x] 在任务审计关联入口中展示映射规则版本和快照摘要。

## 8. 测试与验证

- [x] 增加复杂模板资产解析：多 Sheet、隐藏列、合并单元格、公式依赖和样式摘要。
- [x] 增加字段精确匹配、规范化匹配、候选冲突、类型冲突和字段顺序变化测试。
- [x] 增加公式依赖、非法表达式、循环依赖和拓扑顺序测试。
- [x] 增加模式 A 与模式 B 统一映射视图一致性测试。
- [x] 增加映射快照创建和任务关联测试基础覆盖。
- [x] 增加生成前阻断错误映射和结果 Sheet 追溯测试基础覆盖。
- [x] 运行后端全量测试、前端构建和 `git diff --check`。
